# ========================================
# TrendRadar Docker 部署配置
# ========================================
# 说明：复制此文件为 .env，然后修改相关配置
#      或在 docker-compose.yml 中直接使用这些环境变量

# ========================================
# 核心配置
# ========================================
# 是否启用爬虫功能（必须为 true，否则程序会停止）
ENABLE_CRAWLER=true

# 爬虫请求间隔（毫秒）
CRAWLER_REQUEST_INTERVAL=1000

# 代理配置（如不需要可留空）
USE_PROXY=false
# DEFAULT_PROXY=http://127.0.0.1:10086

# ========================================
# 推送功能控制
# ========================================
# ⭐ 关键配置：设置为 false 禁用所有推送渠道
ENABLE_NOTIFICATION=false

# 报告模式：daily(日报) / current(当前榜单) / incremental(增量)
REPORT_MODE=daily

# ========================================
# 数据显示配置
# ========================================
# 排名高亮阈值（排名在前N的新闻会高亮）
RANK_THRESHOLD=5

# 排序优先级：true=先按配置位置排序，false=先按热点条数排序
SORT_BY_POSITION_FIRST=false

# 每个关键词最大显示新闻数（0=不限制）
MAX_NEWS_PER_KEYWORD=0

# 内容顺序：false=热点词汇统计在前，true=新增热点新闻在前
REVERSE_CONTENT_ORDER=false

# ========================================
# 定时执行配置
# ========================================
# 运行模式：cron(定时) / once(单次)
RUN_MODE=cron

# Cron 表达式（每5分钟执行一次）
CRON_SCHEDULE=*/5 * * * *

# 容器启动时是否立即执行一次
IMMEDIATE_RUN=true

# ========================================
# Web 服务器配置（可选）
# ========================================
# 是否启用 Web 服务器
ENABLE_WEBSERVER=false

# Web 服务器端口
WEBSERVER_PORT=8080

# ========================================
# 时区设置
# ========================================
TZ=Asia/Shanghai

# ========================================
# 推送时间窗口（可选，不配置推送时无意义）
# ========================================
PUSH_WINDOW_ENABLED=false
PUSH_WINDOW_START=09:00
PUSH_WINDOW_END=18:00
PUSH_WINDOW_ONCE_PER_DAY=false

# ========================================
# 所有推送渠道配置（此配置中全部留空）
# ========================================
# 飞书
FEISHU_WEBHOOK_URL=

# 钉钉
DINGTALK_WEBHOOK_URL=

# 企业微信
WEWORK_WEBHOOK_URL=
WEWORK_MSG_TYPE=markdown

# Telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# 邮件
EMAIL_FROM=
EMAIL_PASSWORD=
EMAIL_TO=
EMAIL_SMTP_SERVER=
EMAIL_SMTP_PORT=

# ntfy
NTFY_SERVER_URL=https://ntfy.sh
NTFY_TOPIC=

# Bark
BARK_URL=

# Slack
SLACK_WEBHOOK_URL=
